# lu.sagebl.eu

Deps:
 * Python 3
 * `ninja`
 * `cmark` (if Markdown used)
 * `inotifywait` (if hot-reload used)

Usage:

```console
$ ./build.py
```

Then push `www/` to dest.

Hot-reload:

```console
$ ./hot-reload
```
